apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: intranet-newdeal-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: intranet.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: intranet-newdeal-service
            port:
              number: 80
